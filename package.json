{
  "name": "pocket-guide-mobile",
  "version": "0.1.0",
  "description": "Pocket Guide Mobile App - Customized tour generator for travelers",
  "scripts": {
    "fetch-api": "./scripts/fetch-api-spec.sh",
    "generate-api": "./scripts/generate-api-client.sh",
    "update-api": "npm run fetch-api && npm run generate-api"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "swagger-typescript-api": "^13.2.18"
  }
}
